<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@android:color/darker_gray"
    tools:context="com.amrizal.example.unlockslidertester.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!" />

    <!--<com.amrizal.example.unlockslidertester.SlideButton
        android:id="@+id/unlockButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:clickable="false"
        android:max="100"
        android:progressDrawable="@android:color/holo_blue_dark"
        android:thumb="@drawable/thumb_image"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:thumbOffset="8dp"
        android:maxWidth="200dp"
        android:minWidth="200dp"
        android:splitTrack="false">
    </com.amrizal.example.unlockslidertester.SlideButton>-->
    <com.amrizal.example.unlockslidertester.SlideButton
        android:id="@+id/unlockButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:progressDrawable="@drawable/slidetounlock_progress"
        android:background="@android:color/transparent"
        android:clickable="false"
        android:max="100"
        android:minHeight="@dimen/slider_height"
        android:maxHeight="@dimen/slider_height"
        android:minWidth="@dimen/slider_width"
        android:maxWidth="@dimen/slider_width"
        android:paddingStart="100dp"
        android:paddingEnd="100dp"
        android:splitTrack="false"
        android:thumb="@drawable/forward_thumb_image">
    </com.amrizal.example.unlockslidertester.SlideButton>

</RelativeLayout>
